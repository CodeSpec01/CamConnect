import React from 'react'

const CallList = ({ type }: { type: 'upcoming' | 'ended' | 'recordings' }) => {
    return (
        <div>
            Call List
        </div>
    )
}

export default CallList
